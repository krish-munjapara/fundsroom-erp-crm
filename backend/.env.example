# Server Configuration
# Render sets PORT automatically — use 5000 for local development only
PORT=5000
NODE_ENV=development

# Database Configuration
# PostgreSQL connection string (Neon, Render Postgres, or local)
# Example: postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
DATABASE_URL=postgresql://neondb_owner:npg_XuD3PkxSZm2Y@ep-sweet-brook-ax0frf97.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require

# JWT Configuration
# REQUIRED in production — use a long random secret (never commit the real value)
JWT_SECRET=d982c1e1e6f2e416d4d80a1ff6a48eee8d0012676b0c95b400de09c5d8c1f636

# Frontend Configuration
# Local development
FRONTEND_URL=http://localhost:5173
# Production example (set in Render dashboard, do not hardcode here):
# FRONTEND_URL=https://your-app.vercel.app
