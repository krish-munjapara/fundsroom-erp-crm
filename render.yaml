services:
  - type: web
    name: fundsroom-erp-crm-backend
    rootDir: backend
    runtime: node
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
